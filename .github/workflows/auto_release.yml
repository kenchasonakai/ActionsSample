name: Auto Release
on:
  push:
    branches:
      - develop
  workflow_dispatch:

jobs:
  create-auto-pr:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
      checks: read
      repository-projects: read
    steps:
    - name: Generate token
      id: generate_token
      uses: tibdex/github-app-token@v1
      with:
        app_id: ${{ secrets.APP_ID }}
        private_key: ${{ secrets.PRIVATE_KEY }}
        # Optional (defaults to ID of the repository's installation).
        # installation_id: 1337
        # Optional (defaults to the current repository).
        # repository: "owner/repo"
    - name: Use token
      env:
        TOKEN: ${{ steps.generate_token.outputs.token }}
      run: |
        echo "The generated token is masked: ${TOKEN}"
    #   - uses: actions/checkout@v3
    #     with:
    #       ref: develop
    #       fetch-depth: 0
    #   - id: check_pr
    #     name: check_pr
    #     run: |
    #       echo "count=$(gh pr list --search 'スプリント用プルリクエスト in:title' -B main | wc -l)" >> $GITHUB_OUTPUT
    #     env:
    #       GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    #   - uses: actions/create-github-app-token@v1
    #     id: app-token
    #     with:
    #       app-id: ${{ secrets.APP_ID_GITUSER }}
    #       private-key: ${{ secrets.APP_GITUSER_PRIVATE_KEY }}
    #       owner: ${{ github.repository_owner }}
    #       repositories: |
    #         ActionsSample
    #     env:
    #       GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    #   - name: Get GitHub App User ID
    #     id: get-user-id
    #     run: echo "user-id=$(gh api "/users/${{ steps.app-token.outputs.app-slug }}[bot]" --jq .id)" >> "$GITHUB_OUTPUT"
    #     env:
    #       GH_TOKEN: ${{ steps.app-token.outputs.token }}
    #   - name: git pull develop diff to release
    #     id: create_pr
    #     if: steps.check_pr.outputs.count == 0
    #     run: |
    #       git config --global user.name '${{ steps.app-token.outputs.app-slug }}[bot]'
    #       git config --global user.email '${{ steps.get-user-id.outputs.user-id }}+${{ steps.app-token.outputs.app-slug }}[bot]@users.noreply.github.com>'
    #       git switch -c sprint/$(date +%Y-%m-%d)_$(date +%Y-%m-%d --date "1 week")
    #       git commit --allow-empty -m "プルリクエスト作成用のcommit"
    #       git push origin sprint/$(date +%Y-%m-%d)_$(date +%Y-%m-%d --date "1 week")
    #       gh pr create -B "develop" -t "スプリント用プルリクエスト" -b "今スプリントで開発した内容はここのプルリクエストのブランチ宛にマージしてください"
    #       echo "pull_request_url=$(gh pr view sprint/$(date +%Y-%m-%d)_$(date +%Y-%m-%d --date "1 week") | grep -o 'https://github.com/[^ ]*' | awk 'NR==1')" >> $GITHUB_OUTPUT
    #     env:
    #       GH_TOKEN: ${{ steps.app-token.outputs.token }}
    #   - name: approve PR
    #     if: steps.create_pr.outcome == 'success'
    #     run: |
    #       gh pr review --approve "$(gh pr view sprint/$(date +%Y-%m-%d)_$(date +%Y-%m-%d --date "1 week") | grep -o '
    #     env:
    #       GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
